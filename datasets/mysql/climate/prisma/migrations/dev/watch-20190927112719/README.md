# Migration `watch-20190927112719`

This migration has been generated by Daisy T at 9/27/2019, 11:27:19 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `climate2`.`City`(`city` varchar(191) NOT NULL DEFAULT '' ,`country` varchar(191) NOT NULL DEFAULT '' ,`id` varchar(191) NOT NULL  ,PRIMARY KEY (`id`))
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `climate2`.`MonthlyAvg`(`dryDays` Decimal(65,30) NOT NULL DEFAULT 0 ,`high` Decimal(65,30) NOT NULL DEFAULT 0 ,`id` varchar(191) NOT NULL  ,`low` Decimal(65,30) NOT NULL DEFAULT 0 ,`month` varchar(191) NOT NULL DEFAULT 'Jan' ,`rainfall` Decimal(65,30) NOT NULL DEFAULT 0 ,`snowDays` Decimal(65,30) NOT NULL DEFAULT 0 ,PRIMARY KEY (`id`))
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `climate2`.`MonthlyAvg` ADD COLUMN `city` varchar(191)  ,
ADD FOREIGN KEY (`city`) REFERENCES `climate2`.`City`(`id`) ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190927112719
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,41 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "mysql"
+  url      = "mysql://prisma:prisma@localhost:3306/climate2"
+}
+
+model City {
+  id               String   @default(cuid()) @id
+  city             String
+  country          String
+  monthlyAvgs      MonthlyAvg[]
+}
+
+model MonthlyAvg {
+  id               String   @default(cuid()) @id
+  month            Month
+  high             Float
+  low              Float
+  dryDays          Float
+  snowDays         Float
+  rainfall         Float
+  city             City       
+}
+
+enum Month {
+  Jan
+  Feb
+  March
+  April
+  May
+  June
+  July
+  Aug
+  Sept
+  Oct
+  Nov
+  Dec
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190927112719)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190927112719'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
