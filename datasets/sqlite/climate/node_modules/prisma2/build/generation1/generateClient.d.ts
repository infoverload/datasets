import { GeneratorConfig } from '@prisma/cli';
import { Dictionary } from '../runtime/utils/common';
export interface GenerateClientOptions {
    datamodel: string;
    datamodelPath?: string;
    browser?: boolean;
    cwd?: string;
    transpile?: boolean;
    runtimePath?: string;
    binaryPath?: string;
    outputDir: string;
    platforms?: string[];
    pinnedPlatform?: string;
    version?: string;
    generator?: GeneratorConfig;
}
export declare function buildClient({ datamodel, datamodelPath, cwd, transpile, runtimePath, browser, binaryPath, outputDir, generator, platforms, version, pinnedPlatform, }: GenerateClientOptions): Promise<Dictionary<string>>;
export declare function generateClient({ datamodel, datamodelPath, cwd, outputDir, transpile, runtimePath, browser, binaryPath, platforms, pinnedPlatform, version, generator, }: GenerateClientOptions): Promise<void>;
