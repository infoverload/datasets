import { Command, Env, HelpError, Dictionary, GeneratorDefinitionWithPackage } from '@prisma/cli';
/**
 * $ prisma migrate new
 */
export declare class Generate implements Command {
    private readonly env;
    private readonly generators;
    static new(env: Env, generators: Dictionary<GeneratorDefinitionWithPackage>): Generate;
    private static help;
    private constructor();
    parse(argv: string[], minimalOutput?: boolean): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
