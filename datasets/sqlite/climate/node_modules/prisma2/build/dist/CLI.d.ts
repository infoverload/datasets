import { Command, Commands } from '@prisma/cli';
/**
 * CLI command
 */
export declare class CLI implements Command {
    private readonly cmds;
    static new(cmds: Commands): CLI;
    private constructor();
    parse(argv: string[]): Promise<string | Error>;
    private help;
    private static help;
}
