import { Command, HelpError } from '@prisma/cli';
/**
 * $ prisma COMMAND_NAME docs
 */
export declare class Docs implements Command {
    private readonly commandName;
    private readonly url;
    static new(commandName: string, url: string): Docs;
    private static help;
    private constructor();
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
}
